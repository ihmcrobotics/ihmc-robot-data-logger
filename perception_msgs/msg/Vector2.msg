# A vector in 2D space that represents a direction only.
# Field layout matches Foxglove's foxglove_msgs/msg/Vector2 so this can be used as the
# cell_size of a Grid message that Foxglove's 3D panel will recognize natively.

# x component
float64 x

# y component
float64 y
